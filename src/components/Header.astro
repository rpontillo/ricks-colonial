---
import { site } from '../data/site';

const navItems: ReadonlyArray<readonly [label: string, href: string]> = [
  ['Home', '/'],
  ['Services', '/services'],
  ['Gallery', '/gallery'],
  ['About', '/about'],
  ['Reviews', '/reviews'],
  ['Contact', '/contact'],
];
const currentPath = Astro.url.pathname;
const isActive = (href: string) => href === '/' ? currentPath === '/' : currentPath.startsWith(href);
---
<header class="sticky top-0 z-50 border-b border-navy-900/10 bg-warm-50/95 backdrop-blur-xl" data-site-header data-scrolled="false">
  <div class="site-container flex min-h-19 items-center justify-between gap-6">
    <a href="/" class="flex items-center gap-3" aria-label={`${site.name} home`}>
      <span class="grid size-11 place-items-center rounded-full border border-gold-500/35 bg-navy-950 font-display text-xl font-bold text-white shadow-sm" aria-hidden="true">R</span>
      <span class="leading-tight">
        <strong class="block font-display text-lg text-navy-950">Rick's Colonial</strong>
        <span class="text-xs font-bold uppercase tracking-[0.18em] text-gold-500">Painting</span>
      </span>
    </a>

    <nav class="hidden items-center gap-7 lg:flex" aria-label="Primary navigation">
      {navItems.map(([label, href]) => (
        <a
          href={href}
          aria-current={isActive(href) ? 'page' : undefined}
          class:list={[
            'nav-link text-sm font-bold transition-colors hover:text-gold-500 focus-visible:text-gold-500',
            isActive(href) ? 'text-gold-500' : 'text-navy-900',
          ]}
        >{label}</a>
      ))}
    </nav>

    <div class="hidden items-center gap-4 md:flex">
      <a class="text-sm font-extrabold text-navy-900" href={`tel:${site.phoneHref}`}>{site.phoneDisplay}</a>
      <a class="button-primary" href="/contact">Free Estimate</a>
    </div>

    <button class="menu-toggle grid size-11 place-items-center rounded-full border border-navy-900/15 text-navy-900 transition-colors hover:border-gold-500 hover:text-gold-500 lg:hidden" aria-controls="mobile-menu" aria-expanded="false" type="button">
      <span class="sr-only">Open navigation</span>
      <svg class="size-5" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h16M4 12h16M4 17h16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-width="2"/></svg>
    </button>
  </div>

  <div id="mobile-menu" class="hidden border-t border-navy-900/10 bg-warm-50/98 shadow-xl lg:hidden">
    <nav class="site-container grid gap-1 py-4" aria-label="Mobile navigation">
      {navItems.map(([label, href]) => (
        <a href={href} aria-current={isActive(href) ? 'page' : undefined} class:list={['rounded-xl px-4 py-3 font-bold transition-colors hover:bg-sand-100', isActive(href) ? 'bg-sand-100 text-gold-500' : 'text-navy-900']}>{label}</a>
      ))}
      <a href="/contact" class="button-primary mt-3">Free Estimate</a>
    </nav>
  </div>
</header>

<script>
  const initializeHeader = () => {
    const header = document.querySelector<HTMLElement>('[data-site-header]');
    const toggle = document.querySelector<HTMLButtonElement>('.menu-toggle');
    const menu = document.querySelector<HTMLElement>('#mobile-menu');
    if (!header || !toggle || !menu || toggle.dataset.initialized === 'true') return;

    toggle.dataset.initialized = 'true';
    const updateHeader = () => {
      header.dataset.scrolled = String(window.scrollY > 12);
    };
    window.addEventListener('scroll', updateHeader, { passive: true });
    updateHeader();
    const closeMenu = () => {
      toggle.setAttribute('aria-expanded', 'false');
      toggle.querySelector('.sr-only')!.textContent = 'Open navigation';
      menu.classList.add('hidden');
    };

    toggle.addEventListener('click', () => {
      const willOpen = toggle.getAttribute('aria-expanded') !== 'true';
      toggle.setAttribute('aria-expanded', String(willOpen));
      toggle.querySelector('.sr-only')!.textContent = willOpen ? 'Close navigation' : 'Open navigation';
      menu.classList.toggle('hidden', !willOpen);
    });

    menu.addEventListener('click', (event) => {
      if ((event.target as HTMLElement).closest('a')) closeMenu();
    });

    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') closeMenu();
    });
  };

  document.addEventListener('astro:page-load', initializeHeader);
  initializeHeader();
</script>
